CREATE DATABASE Market;
USE Market;

CREATE TABLE Product (
    product_name VARCHAR(50),
    product_price DECIMAL(10, 2),
    product_inventory INT,
    product_id INT PRIMARY KEY AUTO_INCREMENT    kmkmkmk
);